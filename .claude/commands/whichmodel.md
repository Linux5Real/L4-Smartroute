Use the model-selector MCP tool `analyze_task` with the following prompt: "$ARGUMENTS"

Return the recommendation clearly: which model, which effort level, and why (complexity score + affected communities).
